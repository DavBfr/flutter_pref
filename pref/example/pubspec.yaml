name: example
description: A new Flutter project.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=2.12.0-0 <3.0.0"
  flutter: ">=1.16.0"

dependencies:
  flutter:
    sdk: flutter
  logging:
  pref:

dependency_overrides:
  pref:
    path: ..


dev_dependencies:
  pedantic:

flutter:
  uses-material-design: true
